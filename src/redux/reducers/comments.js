const initialState = {
  items: [],
  isLoading: false,
};

export function commentsReducer(state = initialState, action) {
  switch (action.type) {
    // ТУТ ДОБАВИТЬ ВСЕ ЭКШЕНЫ

    default:
      return state;
  }
}
